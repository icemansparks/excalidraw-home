services:
  excalidraw:
    build: .
    ports:
      - '127.0.0.1:3000:80'
    volumes:
      - ./data:/app/data
    environment:
      - PORT=4000
      - NODE_ENV=production
      - DB_PATH=/app/data/database.sqlite
    restart: unless-stopped
